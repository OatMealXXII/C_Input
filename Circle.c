#include <stdio.h>

int main() {
    // ชนิดข้อมูล ชื่อตัวแปร
    // สร้างตัวแปร ชื่อ pi ชนิดข้อมูล
    float pi;
    pi = 3.14159265;
    printf("pi = %f\n",pi);
    int number = 8;
    printf("number = %d\n",number);
    char alphabet = 'O';
    printf("alphabet = %c\n",alphabet);
    char name[] = "Phongthep";
    printf("name = %s\n",name);
}